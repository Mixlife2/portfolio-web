---
// Props: title, price, features (array), link
const { title, price, features, link } = Astro.props;
---

<div class="bg-white rounded-2xl shadow-lg hover:shadow-xl transition-shadow duration-300 p-6 flex flex-col justify-between border border-gray-200">
    <div>
      <h2 class="text-2xl font-bold text-gray-800 mb-2">{title}</h2>
      <p class="text-indigo-600 text-2xl font-semibold mb-4">${price} USD</p>
      <ul class="list-disc list-inside space-y-1 text-gray-700 mb-6">
        {features.map((f) => <li>{f}</li>)}
      </ul>
    </div>
    <a href={link} target="_blank" class="mt-auto inline-block text-center bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700 transition">
      Ver ejemplo
    </a>
  </div>
  
